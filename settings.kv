#:kivy 2.0

<SettingScreen>:
    TextInput:
        id: username
        size_hint: None, None
        size: 100, 50
        pos: 500, 500

    Button:
        text: 'Save'
        size_hint: None, None
        size: 100, 50
        pos: 804, 10

    Button:
        text: 'Back to menu'
        size_hint: None, None
        size: 100, 50
        pos: 914, 10
        on_press:
            root.manager.transition.direction = 'right'
            root.manager.current = 'menu'